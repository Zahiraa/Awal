<form method="post" action="{{ path('app_admin_terme_delete', {'id': terme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ terme.id) }}">
    <button class="btn">Delete</button>
</form>
