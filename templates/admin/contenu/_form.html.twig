{{ form_start(form) }}{# 🔹 TITLE #}<div class="mb-6">
	{{ form_label(form.title, 'العنوان', {
        label_attr: { class: 'block text-sm font-semibold text-gray-700 mb-2' }
    }) }}
	{{ form_widget(form.title, {
    attr: {
        class: 'w-full rounded-lg border-gray-300 shadow-sm px-3 py-2 text-gray-700 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-teal-500 focus:border-teal-500'
    }
}) }}
	{{ form_errors(form.title) }}</div>{# 🔹 CONTENU NUMEROS (CHECKBOXES) #}<div class="mb-8">


	<div class="mb-8">
		<h3 class="text-sm font-semibold text-gray-700 mb-3">ملف العدد</h3>
		<div class="max-h-60 overflow-y-auto border rounded-md p-2">
			{{ form_widget(form.contenuNumeros) }}
		</div>
		{{ form_errors(form.contenuNumeros) }}
	</div>

	{{ form_errors(form.contenuNumeros) }}</div>{# 🔹 ACTIONS #}<div class="flex flex-col md:flex-row items-center justify-end gap-3 border-t pt-5">
	<a href="{{ path('app_contenu_index') }}" class="w-full md:w-auto bg-white border border-gray-300 px-4 py-2 rounded-lg font-semibold text-gray-700 hover:bg-gray-50 text-center">
		Annuler
	</a>

	{{ form_row(form.draft_button, {
    'attr': {
        'class': 'w-full md:w-auto bg-teal-100 text-teal-700 px-4 py-2 rounded-lg font-semibold flex items-center justify-center gap-2 hover:bg-teal-200 transition'
    }
}) }}

	{{ form_row(form.publish_button, {
    'attr': {
        'class': 'w-full md:w-auto bg-teal-600 text-white px-4 py-2 rounded-lg font-semibold flex items-center justify-center gap-2 hover:bg-teal-700 transition'
    }
}) }}
</div>
{{ form_end(form) }}
