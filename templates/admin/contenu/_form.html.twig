{{ form_start(form, {'attr': {'class': 'w-full p-10 bg-white rounded-3xl shadow-xl border border-gray-100'}}) }}

{# 🔹 TITLE SECTION #}
<div class="mb-10 group">
	{{ form_label(form.title, 'عنوان الإصدار الجديد', {
            label_attr: { class: 'block text-xl font-extrabold text-[#1F2937] mb-4 transition-colors group-focus-within:text-teal-600' }
        }) }}
	<div class="relative">
		{{ form_widget(form.title, {
                attr: {
                    class: 'w-full rounded-2xl border-2 border-gray-100 bg-white/50 backdrop-blur-sm shadow-sm px-6 py-4 text-gray-700 placeholder-gray-400 focus:outline-none focus:ring-4 focus:ring-teal-500/10 focus:border-teal-500 transition-all duration-300 text-lg',
                    placeholder: 'اكتب عنوان العدد هنا...'
                }
            }) }}
	</div>
	<div class="mt-2 px-2">
		{{ form_errors(form.title) }}
	</div>
</div>

{# 🔹 ARTICLES SELECTION SECTION #}
<div class="mb-12 group">
	<div class="flex items-center justify-between mb-4">
		{{ form_label(form.contenuNumeros, 'ملف العدد (المقالات المختارة)', {
                label_attr: { class: 'block text-xl font-extrabold text-[#1F2937] transition-colors group-focus-within:text-teal-600' }
            }) }}
		<span class="text-xs font-bold uppercase tracking-wider text-gray-400 bg-gray-100 px-3 py-1 rounded-full">اختياري</span>
	</div>

	<div class="relative">
		{{ form_widget(form.contenuNumeros, {
                attr: {
                    class: 'js-select2 w-full rounded-2xl border-2 border-gray-100 bg-white shadow-sm p-3 text-gray-700 focus:outline-none focus:ring-4 focus:ring-teal-500/10 focus:border-teal-500 transition-all duration-300 min-h-[120px]'
                }
            }) }}
	</div>

	<p class="mt-3 text-sm text-gray-500 italic px-2">
		💡 نصيحة: يمكنك البحث عن المقالات بكتابة أسمائها في الحقل أعلاه.
	</p>
	<div class="mt-2 px-2">
		{{ form_errors(form.contenuNumeros) }}
	</div>
</div>

{# 🔹 DISCUSSIONS SELECTION SECTION #}
<div class="mb-12 group">
	<div class="flex items-center justify-between mb-4">
		{{ form_label(form.contenuDiscussions, 'حوارات العدد', {
                label_attr: { class: 'block text-xl font-extrabold text-[#1F2937] transition-colors group-focus-within:text-teal-600' }
            }) }}
		<span class="text-xs font-bold uppercase tracking-wider text-gray-400 bg-gray-100 px-3 py-1 rounded-full">اختياري</span>
	</div>

	<div class="relative">
		{{ form_widget(form.contenuDiscussions, {
                attr: {
                    class: 'js-select2 w-full rounded-2xl border-2 border-gray-100 bg-white shadow-sm p-3 text-gray-700 focus:outline-none focus:ring-4 focus:ring-teal-500/10 focus:border-teal-500 transition-all duration-300 min-h-[120px]'
                }
            }) }}
	</div>

	<p class="mt-3 text-sm text-gray-500 italic px-2">
		💡 نصيحة: حدد الحوارات التي تريد ربطها بهذا العدد.
	</p>
	<div class="mt-2 px-2">
		{{ form_errors(form.contenuDiscussions) }}
	</div>
</div>

{# 🔹 ACTION FOOTER #}
<div class="flex flex-col md:flex-row items-center justify-end gap-5 border-t border-gray-100 pt-10">
	<a href="{{ path('app_contenu_index') }}" class="w-full md:w-auto px-8 py-4 rounded-2xl font-bold text-gray-500 hover:text-gray-800 hover:bg-gray-100 transition-all duration-200 text-center">
		العودة للقائمة
	</a>

	<button type="submit" name="{{ form.draft_button.vars.full_name }}" class="w-full md:w-auto bg-white border-2 border-teal-100 text-teal-600 px-8 py-4 rounded-2xl font-bold flex items-center justify-center gap-3 hover:bg-teal-50 hover:border-teal-200 hover:scale-[1.02] active:scale-95 transition-all duration-200">
		<svg class="w-6 h-6" fill="none" stroke="currentColor" viewbox="0 0 24 24">
			<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7H5a2 2 0 00-2 2v9a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-3m-1 4l-3 3m0 0l-3-3m3 3V4"/>
		</svg>
		{{ form.draft_button.vars.label }}
	</button>

	<button type="submit" name="{{ form.publish_button.vars.full_name }}" class="w-full md:w-auto bg-[#F43F5E] text-white px-10 py-4 rounded-2xl font-bold flex items-center justify-center gap-3 hover:bg-[#E11D48] shadow-lg shadow-rose-200 hover:shadow-xl hover:scale-[1.02] active:scale-95 transition-all duration-200">
		<svg class="w-6 h-6" fill="none" stroke="currentColor" viewbox="0 0 24 24">
			<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
		</svg>
		{{ form.publish_button.vars.label }}
	</button>
</div>
{% do form.draft_button.setRendered %}
{% do form.publish_button.setRendered %}

<script>
	$(document).ready(function () {
if ($.fn.select2) {
$('.js-select2').select2({width: '100%', placeholder: 'Sélectionnez une option', allowClear: true});
}
});
</script>

{{ form_end(form) }}
