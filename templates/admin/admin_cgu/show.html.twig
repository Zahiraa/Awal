{% extends 'admin.html.twig' %}


{% block title %} {{ cgu.title }} {% endblock %}


{% block content %}

    <div class="mb-5">

        <div class="flex-1 flex flex-col bg-gray-50 ">
            <header class="bg-white py-6 border-b border-gray-200">
                <div class="flex flex-wrap justify-between items-center gap-4">
                    <div>
                        <h1 class="text-2xl font-semibold text-gray-900">
                            Gestion des conditions générales
                        </h1>
                    </div>
                    <div class="flex items-center space-x-3">
                        <a href="{{ path('app_admin_cgu_index') }}" class="flex  items-center cursor-pointer px-4 py-2 bg-white border border-light-gray-100 text-gray-700 rounded-lg hover:bg-gray-100">
                            Retour
                        </a>

                    </div>
                </div>
                <hr class="text-gray-300 mt-4 mb-2">
            </header>
        </div>
        <!-- CGU Content -->
        {% include 'common/cgu_content.html.twig' with {'cgu': cgu} %}
    </div>

{% endblock %}
