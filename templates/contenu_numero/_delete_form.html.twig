<form method="post" action="{{ path('app_contenu_numero_delete', {'id': contenu_numero.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contenu_numero.id) }}">
    <button class="btn">Delete</button>
</form>
