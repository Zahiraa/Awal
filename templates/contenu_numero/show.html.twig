{% extends 'base.html.twig' %}

{% block title %}ContenuNumero{% endblock %}

{% block body %}
    <h1>ContenuNumero</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contenu_numero.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ contenu_numero.title }}</td>
            </tr>
            <tr>
                <th>SubTitle</th>
                <td>{{ contenu_numero.subTitle }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ contenu_numero.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contenu_numero_index') }}">back to list</a>

    <a href="{{ path('app_contenu_numero_edit', {'id': contenu_numero.id}) }}">edit</a>

    {{ include('contenu_numero/_delete_form.html.twig') }}
{% endblock %}
